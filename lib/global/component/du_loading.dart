import 'package:flutter/material.dart';

class DULoading extends StatelessWidget {
  const DULoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
